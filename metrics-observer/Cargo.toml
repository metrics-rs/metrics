[package]
name = "metrics-observer"
version = "0.1.1-alpha.1"
authors = ["Toby Lawrence <toby@nuclearfurnace.com>"]
edition = "2018"
publish = false

license = "MIT"

[dependencies]
getopts = "0.2"
bytes = "0.5"
crossbeam-channel = "0.5"
prost = "0.6"
prost-types = "0.6"
tui = "0.13"
termion = "1.5"
chrono = "0.4"
sketches-ddsketch = "0.1"
metrics = { version = "0.13.0-alpha.13", path = "../metrics" }
metrics-util = { version = "0.4.0-alpha.10", path = "../metrics-util" }

[build-dependencies]
prost-build = "0.6"
built = "0.4"
